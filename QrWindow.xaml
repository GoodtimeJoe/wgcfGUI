<ui:FluentWindow x:Class="WgcfGUI.QrWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Mobile QR Code Import" Height="500" Width="400"
        WindowStartupLocation="CenterOwner" ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Title="Scan with WireGuard App" Grid.Row="0"/>
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border Background="White" Padding="15" CornerRadius="10" Margin="0,0,0,20">
                <Image x:Name="QrImage" Width="280" Height="280" Stretch="Uniform"/>
            </Border>
            <TextBlock Text="Open WireGuard Mobile App" FontWeight="SemiBold" FontSize="16" TextAlignment="Center"/>
            <TextBlock Text="Click '+' ➔ 'Create from QR Code'" Margin="0,5,0,0" TextAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
